# Main configuration file for training.
defaults:
  - data: default
  - model: default
  - training: default
  - paths: local      # override with paths=jz on Jean-Zay
  - setup: local      # override with setup=jz_<hw> on Jean-Zay
  - experiment: ???   # must be specified at launch
  - _self_

launch_without_submitit: false  # set true for local debugging without submitit
