# Configuration for the windows-setup data profiler (no model, no dataloader).
defaults:
  - setup: local            # setup configs own paths; override with setup=jz_cpu on Jean-Zay
  - windows_setup: pmw_gmi_reconstruction
  - _self_

# Set to true to submit the job via submitit (SLURM).
submitit: false
