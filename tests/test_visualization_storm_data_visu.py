"""Unit tests for storm footprint visualization helpers."""
# Batched-rejection tests removed: Source is now always non-batched (numpy-based).
# TorchSource is the batched ML class and is not accepted by visualization helpers.
