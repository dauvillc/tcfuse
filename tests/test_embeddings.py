"""Unit tests for source embedding modules (to be filled as embedders are implemented)."""

# TODO: add tests for each embedder in src/model/embeddings/
# Convention: each test constructs a synthetic (B, N, C) tensor — no real data.
